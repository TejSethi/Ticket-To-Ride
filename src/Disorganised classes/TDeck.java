/**
 * The deck plus the 5 spread out cards next to it
 */

/**
public class TDeck {

    private COLOR[] colors;
    public List<Card> revealed;
    public List<Card> hidden

    public void TDeck() {
        this.colors = COLOR.values();
        this.revealed = new ArrayList<TCard>;
        this.hidden = new ArrayList<TCard>;
        for (int i = 0; i < this.colors.length; i++) {
            for (int j = 0; j < Settings.get("num_each_color"); j++) {
                this.hidden.add(TCard(this.colors[i]))
            }
        }
    }
}*/